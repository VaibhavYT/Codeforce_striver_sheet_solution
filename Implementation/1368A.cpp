#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    int a, b, n;
    int sum = a + b;
    int count = 0;
    while (t--)
    {

        cin >> a >> b >> n;
    }

    for(int i=0; )


    cout << count << endl;
    return 0;
}